# Copy to .env (gitignored) for local runs. CI and production set these as real
# environment variables instead. Generate a secret with: openssl rand -base64 48
TEAMVAULT_JWT_SECRET=change-me-to-at-least-32-random-characters
